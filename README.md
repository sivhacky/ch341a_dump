# ch341a_dump
Python automation tool for secure, read-only firmware dumping using the CH341A SPI programmer and flashrom. Automatically triggers SPI flash extraction, generates logs, verifies integrity via SHA256/MD5, and produces metadata to support embedded device security testing and firmware analysis.
